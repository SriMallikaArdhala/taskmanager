[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.install]
cmds = ["mvn clean package -DskipTests"]

[start]
cmd = "java -Dspring.profiles.active=prod -jar target/taskmanager-backend-0.0.1-SNAPSHOT.jar"